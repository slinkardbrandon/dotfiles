[general]
import = ["common.toml", "colors.toml"]

[terminal.shell]
program = "wsl.exe"
args = ["~", "-d", "Ubuntu"]

[window]
opacity = 0.97
padding = { x = 8, y = 8 }
dynamic_title = true

[scrolling]
history = 10000
